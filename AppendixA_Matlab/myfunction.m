function [c,d] = myfunction(a,b)
    % Returns the sum and difference of
    % its arguments. You can type
    % help(myfunction) in the command
    % window to display this comment
    % block.

    c = a + b;
    d = a - b;
    
end